<UserControl 
    x:Class="StringTheory.UI.ReferrerTree"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:StringTheory.UI"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:MainWindow}">

    <UserControl.Resources>

        <HierarchicalDataTemplate DataType="{x:Type local:ReferrerNode}" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Title}" />
        </HierarchicalDataTemplate>

    </UserControl.Resources>

    <TreeView
        ItemsSource="{Binding ReferrerTree.Roots}"
        TreeViewItem.Expanded="OnItemExpanded" />

</UserControl>